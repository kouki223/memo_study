# main.py　リトライロジックとエラーハンドリング(引数)

少し面倒な処理は、例外と成功時のメッセージを構築するLambdaかも？？

## 処理を言語化

- 各種グローバル定数の定義
    - .......

- mainメソッド(エントリーポイント)
    - initialize_chrome_driverメソッド
        - ユニークなディレクトリとchrome driverを定義するメソッド
    - get_company_meta_dataメソッド
        - ステートマシンでS3から取得したJSONを企業単位で分割したCOMPANY_META_DATAデータをcompany_meta_dataに格納して返すメソッド
    - get_retry_count_data
        - リトライ回数ををS3から取得した企業情報単位でJSON形式になっている情報がmapで回っているステートマシン経由で受け取る
    - get_scout_sending_info
        - S3から取得したスカウトに関する企業情報がJSON形式になっている情報がmapで回っているステートマシン経由で受け取る
    - get_execution_start_time
        - 実行開始時刻を取得するメソッド
    - transmission_slot
        - scout_sending_infoから"transmission_slot"プロパティの値を取得する
            - スカウト送信数を受け取る(スカウト処理内で繰り返し処理する回数)
    - get_fargate_metadataメソッド
        - fargateが内部的に生成するタスクメタデータエンドポイント
            - task_arnやtask_id,cluster,task_family,task_versionなどを取得して変数responseに格納する
            - 変数responseをJSON形式に変換して呼び出し元に返す
    - create_fargate_infoメソッド
        - aws.utilsに定義しているメソッド
            - 企業単位の情報 => company_meta_data
            - fargateの内部エンドポイント経由で取得したfargateのメタデータをJSON形式にして変数fargate_infoに格納して返すメソッド
    - account_info
        - S3から取得したデータを企業単位でmapで受け渡した情報の中からログイン情報を取得して、変数account_infoへ格納する
    - target_year
        - 企業情報から対象年度の情報を取得する

    - login_to_siteメソッド(offer boxへログインするメソッド)
        - 例外が起きた場合には、例外を捕捉して、RuntimeErrorを返す
            - 入力処理
                - config.jsonにofferboxとして定義しているURLをグローバル関数経由で取得する
                - form_LOGINIDとform_PASSWORDに対して行う
                    - {セレクタ}が存在するか、10秒間待機する
                    - find_elementをfind_elementに対して実行して、cd_inputに結果を代入する
                    - cd_inputに対してsend_keysを実行する
            - クリック処理
                - form_submitに対してWebDriverWaitする
                    - 変数login_btnへfind_elementを行った結果を代入する
                    - 変数login_btnにclickメソッドを実行する
        - switch_target_yearメソッド(変数is_target_year_switchedにswitch_target_yearメソッドの返り値を格納する)
            - 変数before_selected_yearに現在のinput要素のname属性にactiveyearが付いているかつ、状態がcheckedの要素のvalueを格納する
                - もしもここでUnexpectedAlertPresentExceptionを捕捉した場合
                    - ログを出力してaccept_alert_and_logメソッドを実行する
                        - 予測しているアラート内容のTEXTだった場合には例外を握り潰してループ処理へ戻る
                        - 予測していないアラート内容だった場合には例外をスローして、Falseを返す
                - 変数target_yearと変数before_selected_yearを等比較する
                    - Trueの場合Trueを返してループ処理終了
                    - Falseの場合順次進行する
            - 変数year_selecterに親要素がliのidにcompany_activeyear_changeを持つul要素を対象にWebDriverWaitをした戻り値を代入する
            - 変数expandedにexecute_with_alert_recoveryメソッドにActionChains(driver).move_to_element(year_selecter).click().perform()を引数として渡して実行した結果を代入する
                - actionとして引数に渡した処理を実行する
                    - その後、Trueを返す
                    - もしも、例外を捕捉した場合は、accept_alert_and_logを実行して捕捉したエラー通知が予測されたエラーなのか？予測されていないエラー内容なのかを判別して処理を分岐する
                        - 予測した例外の場合
                            - alertをacceptして
                            - Trueを返す
                        - 予測していない例外の場合
                            - alertを閉じずにFalseを返す
                                - もう一度、switch_target_yearのループに戻る
            - もし、expandedが
            1   - falseの場合には127行目のループ処理へ戻る
                - trueの場合には
                    - wait_for_dom_stabilizationメソッドでDOMの安定化を期待する
                        - ここまでで、期待する年度選択のためのに必要な要素が存在する事を確認できた事を保証する
            - 変数radio_buttonsにinput要素にactiveyearをname属性として持つ要素（全ての年度選択input要素）を格納する
                - それらのボタンのvalueの中にtarget_yearと合致するものが存在するか確認する
                    - 合致する要素が存在した場合、合致した要素を変数target_radioへ格納する
                        - もし、期待する要素が存在しなかった場合にはFalseを返す
                            - そもそも期待する年度情報に対応する年度のセレクトが存在しないという意味になるため処理を打ち切る
                                - login_to_siteメソッドへ戻る
                    - execute_with_alert_recoveryにtarget_radioをJSクリックするactionを渡して指定する年度に切り替えを行う
            - 変数after_selected_yearに現在、年度情報としてcheckedが付いているname属性にactiveyearが付いている要素のvalueの要素を代入する
                - 変数after_selected_yearとtarget_yearが合致した場合
                    - Trueを返す
                - UnexpectedAlertPresentExceptionを捕捉した場合には、
                    - accept_alert_and_logメソッドを実行した結果がTrueの場合
                        - もう一度、target_yearとafter_selected_yearを検証する
                            - 検証に成功しなかった場合にはループへ戻る
            - max_attempts過ぎてしまう場合にはfalseを返す
        - is_logged_in(ログイン成功したいるか判別するメソッド)
            - s-navbar__innerをclassnameに持つ要素が存在するかWebDriverWaitで10秒間待機して確認する
                - check_current_yearメソッド
                    - input要素でnameにactiveyearが定義されている要素がcheckedの状態の要素のvalueを取得して変数current_yearに代入する
                        - 変数current_yearと変数target_yearを等比較してTrueであればTrueを返すFalseであればFalseを返す
            - check_current_yearの返り値が
                - Trueの場合
                    - TrueとNoneを返す
                - Falseの場合
                    - Falseとエラーメッセージを返す
    - login_successがfalseの場合にはRuntimeErrorを返す

    - main_handleにログイン直後のウィンドウを保持する
    - 変数skipped_user_idsを定義

    - 変数transmission_slotの数分だけfor文を実行する
        - 開いているwindow_handlesの分だけ繰り返し処理を行う
            - handleがmainのhandleではない場合にはhandleのwindowにswitch_toしてclose処理を行う
        - main_handleにwindowをswitchする
        - navigate_to_student_search_pageの戻り値をsearch_page_successとsearch_page_error_messageに代入する
            - Global定数のSTUDENT_SEARCH_URLとループの数に応じて決定される変数page_numGlobal定数のPER_PAGE_COUNTに応じて決定されるURLに遷移する
            - wait_until_with_alert_recoveryの第2引数に"search-terms--tabs"をCSS_NAMEに存在する要素を探す処理を実行してwaitedと_に戻り値をそれぞれtuple型で代入する
                - waitedがfalseの場合はfalseとエラーメッセージを返す
            - window.scrollTo(0, 0);をJS実行して、ページトップまでスクロールする
            - check_current_yearメソッドで現在の年度を確認する
                - Trueの場合にはTrueとNoneを返す
                - Flaseの場合には、Flaseと"年度チェックに失敗しました。"を返す
        - apply_saved_filtersメソッドの戻り値をapply_filters_successとapply_filters_error_messageに格納する
            - wait_until_with_alert_recoveryメソッドに"nav-tabs"をCSS_NAMEとして持つ要素が表示されているか確認するメソッドのvisibility_of_element_locatedを渡した戻り値をwaitedとnav_tabsに代入する
                - もしも、waitedがfalseの場合にはFalseと"通信エラーアラートを閉じた後もnav-tabs表示待機に失敗しました。"を返す
            - li_elements2よりも小さい場合にはFalseとf"Expected at least 2 li elements, found {len(li_elements)}"を返す
            - 変数second_liにli_elementsの2番目の要素を格納する
            - 変数a_elementに変数second_liのaタグを持つ要素を代入する
            - 変数clickedにexecute_with_alert_recoveryメソッドの第2引数にa_elementのarguments[0]にclick処理を行うJSを実行する処理を渡した戻り値を格納する
            - もしも、変数clickedがFalseだった場合には、False"通信エラーアラートを閉じた後も保存済み検索条件タブのクリックに失敗しました。"を返す
            - 変数waitedと変数reference_termsにwait_until_with_alert_recoveryメソッドにIDとしてreference_termsを持つ要素が表示されているか確認するメソッドであるvisibility_of_element_locatedを第2引数として渡した結果を格納する
            - scroll_to_elementメソッドでreference_termsが画面の上部へ来るようにスクロールする
            - wait_for_search_list_loading_to_finishメソッドでsearch_list_loadingをIDに持つ要素が表示されない状態になるまで待つ
                - loading_elementsがfind_elementsで見つける事が出来ない
                    - Falseを返す
                - loading_elementが存在するが、ディスプレイされていない
                    - Flaseを返す
            - wait_until_with_alert_recoveryメソッドにCLASS_NAMEにcss-p7o7coを持つ要素が表示されている確認する
                - もし、waitedがFalseの場合には、
                    - Falseと"通信エラーアラートを閉じた後も保存済み検索条件リスト取得に失敗しました。"を返す
            - Global定数のAPPLY_SAVED_FILTER_NAMEをtarget_filter_nameに格納する
            - saved_filter_elements(保存済みの検索条件のリスト)の数分だけ、繰り返し処理を行う
                - saved_filter_textにCLASS_NAMEにcss-182kmv3を持つ要素のtext要素のみを取得する
                - available_filtersにappendする
                - target_filter_nameとsaved_filter_textを等比較して合致した場合には
                    - 変数search_buttonにsaved_filter_elementの中でbutton要素かつ、文字列として検索を持っている要素を格納する
                        - 変数search_buttonをクリックする処理をexecute_with_alert_recoveryメソッドの第2引数として渡す
                            - 戻り値にはclickedを定義する
                    - clickedが
                        - falseの場合にはfalseを返してエラーメッセージを返す
                    - clickedがfalseではなかった場合
                        - found_matchフラグをtrueにする
            - found_matchがfalseの場合、falseとエラーメッセージを返す
        - もし、apply_filters_successがfalseの場合にはRuntimeErrorをかえす

        - initialize_search_resultsメソッドを呼び出して、init_success,init_error_message,total_hits に格納している
            - navigate_to_student_search_pageメソッドを引数に1を渡して呼び出す
            - wait_until_with_alert_recoveryメソッドにIDにsearchHitを持つ要素が表示されているか待機する処理を第2引数に渡す
                - waitedがFlaseの場合にはFalse+エラーメッセージ
            - search_hit_elementのTAG_NAMEがemである要素を取得してem_elemに格納する
            - 変数total_hitsに,を空白に入れ替える処理を行なったテキストを格納する
            - True, None, total_hitsを返す
            - 例外の場合にはFalse,エラーメッセージ,0を返す
        - init_successがfalseの場合にはRuntimeerrorを起こす

        - send_scoutメソッドを実行する
        ```
            - 引数
                - company_meta_data => S3から取得した現在処理中の企業に関する情報
                - scout_sending_info => AIスカウトに必要な様々な処理を含む情報
                - execution_start_time => 処理を始めた時間
                - transmission_slot => 最大、何通スカウトを送るか？
                - main_handle => 検索実行したWindow
                - skipped_user_ids => すでにスキップする必要があるユーザーのidリスト
                - total_hits => 検索条件にヒットした学生の数
            - 返り値
                - send_scout_success
                - send_scout_error_message
                - remaining_transmission_slots
        ```
            - total_hitsが0よりも大きい場合にはtotal_pagesの計算を行う
                - total_hitsが0の場合には例外をRuntimeErrorを吐く
            - total_pagesに応じてfor文を実行する
                - 1ページ目はURL遷移なし
                - 2ページ目以降はURL遷移する
                    - navigate_to_student_search_pageにpage_numを渡してページ遷移する
                        - もし、navigate_successがfalseだった場合はRuntimeError
                - wait_until_with_alert_recoveryを呼び出しofferListAreaをIDに持つ要素が表示されていないか確認する
                    - waitedがFalseの場合はRunTimeError
                - offer_list_areaの親要素がCSS_NAMEがlyt-panelかつ、div要素である要素を全て取得する(学生の数分だけ？divが取れる？)

                - 取得したdiv_elementsの数分だけ繰り返し処理を行う
                    - execute_with_alert_recovery_valueメソッドにCSS_SELECTORがli.userid spanである要素のtextを取得する
                - waitedがfalseだった場合は、continueする
                ↑
                ここってなんでcontinueなんだろうか？例外もしくはfalseのような気も、ただ、仕様に応じてない場合もあるのか？？
                - user_idはuser_id_textもしくは、空白になる
                    - NoSuchElementExceptionを捕捉した場合はNoneにした上でcontinueする(ループを抜ける)
                - 変数skipped_user_idsの中にuser_idが含まれないかつ、Noneではない場合にcontinueする
                - (By.CSS_SELECTOR, ".badge.badge-important.offerreachmax")に合致する要素が存在する場合
                    - True => continueする
                    - False => pass
                - ".ofb-btn.ofb-btn--general.gtm_button_to_go_to_prof"の要素を取得する
                    - 学生プロフィール遷移ボタンの取得をする
                        - 取得できなかった場合はcontinue
                - 取得した学生プロフィールボタンが変数buttonとして渡されるため、JSクリックを行う
                    - 失敗した場合continue
                - lambda d: len(d.window_handles) > 1 を wait_until_with_alert_recoveryメソッドの第2引数に渡す事で新しいタブが開くのを待機する
                    - 失敗した場合continue
                - main_handleではないhundleを取得する事で新しいハンドルを取得する
                - 変数_resign_h2_xpathに退会ページに存在する要素を格納
                - # main or 退会済みの場合に存在する要素のどちらかが表示されるか待機
                    - どちらも表示されない場合には
                        - 例外を吐く
                - 



                fargate起動
↓
main.py実行
↓
グローバル定数の定義
↓
ユニークディレクトリとdriver定義
↓
statemachineから処理単位での純粋な企業情報を取得
↓
リトライ回数をmapから受け取る
↓
スカウトを送る数をmapから受け取る
↓
実行開始時間を取得
↓
スカウト送信数を受け取る
↓
fargateのエンドポイント経由でfargateのメタデータを取得してJSON形式にする
↓
login_to_siteメソッド（ログイン処理を実行する）
    ↓
    IDとパスワードを入力する
    ログインボタンをクリックする
    ↓
    switch_target_yearメソッドを呼び出す
        ↓
        現在の年度表示部分が、すでに処理を実行したい年度情報と合致する場合
            ↓
            True => 処理終了
        ↓
        Falseの場合
        ↓
        年度選択欄に存在する全てのul要素を全て取得して対象の年度の要素が存在するか確認
        都度、画面遷移と要素確認の前や、操作形実行時にはラッパーメソッドを使って予期しているアラートを無視する処理を含める
        ↓
        繰り返し処理の中で処理実行したい年度と選択した年度の選択が合致した場合にTrueを返す
        ↓
        もし、繰り返し処理を全て実行した後で合致する要素が存在しない場合には例外を吐く
    ↓
    is_logged_inメソッドを呼び出す
        ↓
        s-navbar__innerをclass_nameに持つ要素を探す
            ↓
            check_current_yearメソッド
            ↓
            現在の年度表示を確認して
                ↓
                一致する場合 => True
                ↓
                一致しない場合 => False
    ↓
    ログイン後の要素が表示されているかつ、現在の年度表示が処理している年度と合致する場合処理を進める
↓
現在のhandleをmainとして保持する   
↓
スカウト送信数の数分だけ繰り返し処理を行う
    ↓
    不要なウィンドウを全て削除して、メインのみの状態にする
    ↓
    学生検索ページへ遷移する
    この時にアラートが出ていないか確認する（ラッパーメソッド）
    ↓
    ページトップへスクロール
    ↓
    check_current_yearメソッドを呼び出して現在の年度表示を確認する
    ↓
    apply_saved_filtersメソッドを実行する(保存済み検索条件リストから検索状態を保持したい条件を保存する処理)
        ↓
        必要な要素の待機を行なった上でローディング要素の消失を待機
        ↓
        全ての保存されている検索状態の中から期待する文字列と一致する検索条件を取得する
            ↓
            もしも、期待する検索条件が存在しない場合には、例外を吐く
        ↓
        検索ボタンをクリックして検索条件を保持する
        ↓
        何らかのエラーが起きている場合には例外を吐く
    ↓
    initialize_search_resultsメソッドで検索条件に合致した学生数をtotal_hitsとして取得する
    ↓
    send_scoutメソッドへ
    ↓

学生1人という単位をどのように定義するか？


[仕様を説明できるようにする]
